https://github.com/easy-install/easy-install


```bash

curl -fsSL https://gh-proxy.com/https://github.com/ahaoboy/ei-assets/blob/main/install.sh | sh -s -- --proxy gh-proxy
curl -fsSL https://cdn.jsdelivr.net/gh/ahaoboy/ei-assets/install.sh | sh -s -- --proxy jsdelivr

```